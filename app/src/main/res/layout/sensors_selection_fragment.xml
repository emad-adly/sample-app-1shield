<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:background="@color/selected_sensors_list_background">


    <Button
        android:layout_width="@dimen/sensors_list_height"
        android:layout_height="@dimen/sensors_list_height"
        android:background="@drawable/add_press_background"
        android:text="+"
        android:textColor="@android:color/white"
        android:textSize="@dimen/sensor_button_text_size"
        android:id="@+id/addSensors"/>

    <ScrollView
        android:layout_width="60dp"
        android:layout_height="wrap_content">
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/sensors_container"
            android:orientation="vertical">
        </LinearLayout>

    </ScrollView>


</LinearLayout>